Casi d'uso:

CreaProfiloPaziente
	Nella specifica: eliminare punto 3 (associa cg), il cg lo associa quando inserisce i dati. Il punto di estensione passa al punto 2. Eliminare punto 4, non c'è nessuna conferma dei dati
	Nel diagramma: aggiungere blocco OPT per creazioneCaregiver. Modificare sintassi nomi classi. il messaggio di update non va direttamente dalla view al model ma passa dal create

CreaProfiloCaregiver
	Nella specifica: splittare passo 1. Eliminare passo 2 (vecchio).
	Nel diagramma: modificare sintassi nomi classi. il messaggio di update non va direttamente dalla view al model ma passa dal create

CreaEsercizio:
	Nella specifica: accorpare passo 2, 3 e 5. Eliminare passo 4 (e anche il 5 ovviamente)
	Nel diagramma: modificare sintassi nomi classi. il messaggio di update non va direttamente dalla view al model ma passa dal create

AggiungiParola:
	Nella specifica: eliminare passo 3
	Nel diagramma: modificare sintassi nomi classi. il messaggio di update non va direttamente dalla view al model ma passa dal create

CreaSessione:
	Nella specifica: diciamo da rifare
	Nel diagramma: OK

SceltaPaziente (logopedista):
	ELIMINARE?

AssegnaSessione:
	Nella specifica: diciamo da rifare
	Nel diagramma: OK

Visualizza Progressi:
	Nella specifica: OK, aggiungere anche caregiver come attore primario (logopedista/caregiver)
	Nel diagramma: mancante

ModificaAccountCaregiver (deve diventare ModificaProfiloCaregiver):
	Nella specifica: il caregiver immette tutti i dati che vuole modificare in un sol colpo e non c'è nessuna conferma, quindi: eliminare passo 2 e 4
	Nel diagramma: OK, l'ho fatto, credo di avertelo passato

SceltaProfilo (logopedista):
ELIMINARE?
Altrimenti:
	Nella specifica: modifica punti di estensione (levare la modifica paziente e inserire visualizzazione progressi)

ModificaAccountPaziente (deve diventare ModificaProfiloPaziente)
	Nella specifica: l'attore è il logopedista, non il cg. Per gli stessi motivi della modifica del cg, eliminare passo 2 e 4. Volendo possiamo specificare che può modificare solo la diagnosi
	Nel diagramma: OK, l'ho fatto, credo di avertelo passato

SvolgiSessione
	Nella specifica: forse c'è da modificare, forse no
	Nel diagramma: OK

ScaricaQuestionario
	Nella specifica: OK
	Nel diagramma: OK

